{
  "service_account_key_file": "{{ env `ACCT_KEY_FILE` }}",
  "folder_id": "{{ env `FOLDER_ID` }}",
  "source_image_family": "ubuntu-2004-lts",
  "zone": "{{ env `ZONE` }}",
  "subnet_id": "{{ env `SUBNET_ID` }}",
  "image_preemptible": "false"
}
